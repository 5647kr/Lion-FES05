// access, search, insertion, deletion

const hash = {two: 2, three: 3 };

// 값을 insertion
hash.one = 1;

// 값을 access
hash.one = 2;
hash.two = 10;

// 값을 search
hash.three;

// 값을 delete
delete hash.three;